<h2 class="page-header">
    Usuarios
</h2>

<div class="panel panel-default with-table" ng-controller="UsuarioController as usuario">
   
    <div class="panel-heading">
        <div class="row">
            <div class="col-md-8">
                <a class="btn btn-primary btn-sm" ng-click="usuario.create(tab)">
                <span class="glyphicon glyphicon-file"></span>Nuevo </a>
            </div>
            <div class="col-md-4">
            </div>
        </div>
    </div>

    <table class="table" style="margin: 4px">
        <tr>
            <th>
                Email
            </th>
            <th>
            </th>
        </tr>
        <tr ng-if="usuario.loading">
            <th>
                Obteniendo datos...
            </th>
            <th>
            </th>
        </tr>
        <tr ng-if="usuario.loading && usuario.items.length == 0">
            <th>
                No hay registros
            </th>
            <th>
            </th>
        </tr>
            <tr ng-repeat="item in usuario.items">
                <td>
                    {{item.Email}}
                </td>
                <td class=" text-right col-md-4 col-lg-4 col-xs-4">
                           
                    <a  ng-click="usuario.detail(tab, item.Id)" class="btn btn-xs btn-default" data-ajax>
                        <span class="glyphicon glyphicon-eye-open"></span>
                        <text class="hidden-xs">Detalles</text>
                    </a>
                    
                    <a  ng-click="usuario.edit(tab, item.Id)" class="btn btn-xs btn-default" data-ajax>
                        <span class="glyphicon glyphicon-pencil"></span>
                        <text class="hidden-xs">Modificar</text>
                    </a>
                            
                    <a ng-click="usuario.deleteUser(tab ,item.Id)" class="btn btn-xs btn-default " data-ajax>
                        <span class="glyphicon glyphicon-remove"></span>
                        <text class="hidden-xs">Eliminar</text>
                    </a>
                </td>
            </tr>
    </table>
</div> 